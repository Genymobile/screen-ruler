//! screen-ruler application entry point.
//!
//! This is currently a placeholder binary. The Slint UI and overlay
//! feasibility spike will replace
//! this with the real windowed application once the backend modules from
//! `ruler-core` are ported.

fn main() {
    println!("{} scaffold - UI not yet implemented", ruler_core::CRATE_NAME);
}
